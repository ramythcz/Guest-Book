server:
    port: 8080

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/guestbook
        username: root
        password:
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            show_sql: true
            format_sql: true